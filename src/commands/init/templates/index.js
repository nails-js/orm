const nailsrcConfig = require("./nailsrc.template");
const dbConfig = require("./db-config.template");
const index = require("./index.template");

module.exports = { nailsrcConfig, dbConfig, index };
